const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const query=scope=>scope.kind==='global'?'scope=global':`scope=project&projectID=${encodeURIComponent(scope.projectID)}`;
const applies=(entry,projectID)=>entry.scope==='global'||entry.projectID===projectID;

export function mountPlaybooks({host,scope,projects,api,notify}){
 let disposed=false,pending=false,dirty=false,state=null,skills=null,connections=null,selected=null,editor=null,provider='codex';const guard=()=>!disposed&&host.isConnected,$=selector=>host.querySelector(selector);
 const fail=error=>{if(guard()){$('[data-playbook-error]').textContent=error.message;notify?.(error.message);}};
 const mark=value=>{dirty=value;if($('[data-save-playbook]'))$('[data-save-playbook]').disabled=!dirty||pending;if($('[data-save-default]'))$('[data-save-default]').disabled=!dirty||pending;};
 async function load(){pending=true;host.innerHTML='<div class="resource-empty"><h2>Loading Playbooks…</h2></div>';try{[state,skills,connections]=await Promise.all([api('playbooks?'+query(scope)),api('skills?'+query(scope)),api('connections?'+query(scope))]);if(!guard())return;selected=state.entries.find(item=>item.id===selected?.id)||state.entries.find(item=>!item.archived)||state.entries[0]||null;editor=null;dirty=false;render();}catch(error){if(guard())host.innerHTML=`<div class="resource-empty"><h2>Playbooks unavailable</h2><p>${esc(error.message)}</p><button data-retry>Retry</button></div>`,$('[data-retry]').onclick=load;}finally{pending=false;}}
 const currentDefault=()=>state.defaults.find(item=>item.projectID===scope.projectID&&item.provider===provider)||{revision:0,playbookID:''};
 function render(){if(!guard())return;const rows=state.entries,available=rows.filter(item=>!item.archived&&item.providers.includes(provider)&&applies(item,scope.projectID));host.innerHTML=`<div class="resource-toolbar playbook-toolbar"><div><p>Reusable managed skills and MCP selections. Models, effort, workspace, and task stay with each session.</p></div><button class="primary" data-new-playbook>New playbook</button><button data-refresh>Refresh</button></div><p class="form-error" data-playbook-error role="alert"></p>${scope.kind==='project'?`<section class="resource-policy playbook-default"><div><span class="eyebrow">NEXT SESSION</span><h2>Project default</h2><p>Used when a session does not explicitly choose another playbook.</p></div><label>Provider<select data-default-provider><option value="codex">Codex</option><option value="claude">Claude</option></select></label><label>Playbook<select data-default-playbook><option value="">Legacy project defaults</option>${available.map(item=>`<option value="${esc(item.id)}">${esc(item.name)} · v${item.version}</option>`).join('')}</select></label><button data-save-default disabled>Save default</button></section>`:''}<div class="resource-layout"><section class="resource-list" aria-label="Playbooks">${rows.map(item=>`<button class="resource-row ${selected?.id===item.id?'selected':''}" data-playbook-id="${esc(item.id)}"><span><strong>${esc(item.name)}</strong><small>${item.scope==='global'?'Global':projects.find(project=>project.id===item.projectID)?.name||'Project'} · ${item.providers.join(' + ')} · v${item.version}</small></span><span class="resource-state">${item.archived?'Archived':`${item.skillIDs.length} skills · ${item.connectionIDs.length} MCP`}</span></button>`).join('')||'<div class="resource-empty"><h2>No playbooks</h2><p>Create one from here or save one from a session.</p></div>'}</section><aside class="resource-detail">${editor?editorMarkup():detailMarkup(selected)}</aside></div>`;if(scope.kind==='project'){$('[data-default-provider]').value=provider;$('[data-default-playbook]').value=currentDefault().playbookID||'';}bind();}
 function detailMarkup(item){if(!item)return '<div class="resource-empty"><h2>Select a playbook</h2><p>Inspect its managed capabilities and version history.</p></div>';const source=item.source?.kind==='session'?`Session ${item.source.sessionID}`:item.source?.kind==='playbook'?`Playbook ${item.source.playbookID} v${item.source.version}`:'Created in Workbench';return `<span class="eyebrow">${item.scope.toUpperCase()} · VERSION ${item.version}</span><h2>${esc(item.name)}</h2><p>${esc(item.description||'No description.')}</p><dl class="resource-facts"><div><dt>Status</dt><dd>${item.archived?'Archived':'Available'}</dd></div><div><dt>Providers</dt><dd>${esc(item.providers.join(', '))}</dd></div><div><dt>Skills</dt><dd>${item.skillIDs.length}</dd></div><div><dt>Connections</dt><dd>${item.connectionIDs.length}</dd></div><div><dt>Provenance</dt><dd>${esc(source)}</dd></div><div><dt>History</dt><dd>${item.history.length} prior version${item.history.length===1?'':'s'}</dd></div></dl><div class="resource-actions"><button data-edit>Edit</button><button data-duplicate>Duplicate</button><button data-archive>${item.archived?'Restore':'Archive'}</button></div>`;}
 function editorMarkup(){const item=editor.item||{},projectOptions=projects.filter(project=>project.id!=='unassigned'),chosenProviders=item.providers||['codex'],chosenSkills=new Set(item.skillIDs||[]),chosenConnections=new Set(item.connectionIDs||[]),managed=skills.managed.filter(skill=>!skill.archived&&(item.scope==='project'?applies(skill,item.projectID):scope.kind==='project'?applies(skill,scope.projectID):skill.scope==='global')),connectionRows=connections.connections.filter(connection=>!connection.shadowed&&connection.configured&&(item.scope==='project'?(!connection.projectID||connection.projectID===item.projectID):scope.kind==='project'?(!connection.projectID||connection.projectID===scope.projectID):!connection.projectID));return `<form data-editor><span class="eyebrow">${item.id?'EDIT PLAYBOOK':'NEW PLAYBOOK'}</span><h2>${item.id?'Edit playbook':'Create playbook'}</h2><label>Name<input name="name" maxlength="100" required value="${esc(item.name||'')}"></label><label>Description<textarea name="description" maxlength="1000" rows="3">${esc(item.description||'')}</textarea></label><label>Scope<select name="scope"><option value="global" ${item.scope==='global'||(!item.scope&&scope.kind==='global')?'selected':''}>Global</option>${projectOptions.map(project=>`<option value="project:${esc(project.id)}" ${(item.projectID||scope.projectID)===project.id?'selected':''}>${esc(project.name)}</option>`).join('')}</select></label><fieldset><legend>Providers</legend><label><input type="checkbox" name="provider" value="codex" ${chosenProviders.includes('codex')?'checked':''}> Codex</label><label><input type="checkbox" name="provider" value="claude" ${chosenProviders.includes('claude')?'checked':''}> Claude</label></fieldset><fieldset><legend>Managed skills</legend><div class="resource-checklist">${managed.map(skill=>`<label><input type="checkbox" data-editor-skill="${esc(skill.id)}" ${chosenSkills.has(skill.id)?'checked':''}><span><strong>${esc(skill.name)}</strong><small>${skill.scope} · v${skill.version}</small></span></label>`).join('')||'<p>No eligible managed skills.</p>'}</div></fieldset><fieldset><legend>MCP connections</legend><div class="resource-checklist">${connectionRows.map(connection=>`<label><input type="checkbox" data-editor-connection="${esc(connection.id)}" data-provider="${esc(connection.provider)}" ${chosenConnections.has(connection.id)?'checked':''}><span><strong>${esc(connection.name)}</strong><small>${esc(connection.provider)} · ${esc(connection.source)}</small></span></label>`).join('')||'<p>No eligible managed connections.</p>'}</div></fieldset><p class="field-help">Selections are exact, including none. Native provider skills and configuration are separate from this managed playbook.</p><div class="resource-actions"><button type="button" data-cancel>Cancel</button><button class="primary" data-save-playbook ${dirty?'':'disabled'}>Save playbook</button></div></form>`;}
 function bind(){$('[data-new-playbook]').onclick=()=>{editor={item:{scope:scope.kind,projectID:scope.projectID,providers:['codex'],skillIDs:[],connectionIDs:[]},requestKey:crypto.randomUUID()};dirty=false;render();$('[name="name"]')?.focus();};$('[data-refresh]').onclick=()=>dirty?notify?.('Save or cancel the current draft before refreshing.'):load();host.querySelectorAll('[data-playbook-id]').forEach(button=>button.onclick=()=>{if(dirty)return notify?.('Save or cancel the current draft first.');selected=state.entries.find(item=>item.id===button.dataset.playbookId);editor=null;render();});$('[data-edit]')?.addEventListener('click',()=>{editor={item:structuredClone(selected)};dirty=false;render();});$('[data-cancel]')?.addEventListener('click',()=>{editor=null;dirty=false;render();});$('[data-editor]')?.addEventListener('input',()=>mark(true));$('[data-editor]')?.addEventListener('submit',save);$('[data-archive]')?.addEventListener('click',archive);$('[data-duplicate]')?.addEventListener('click',duplicate);if(scope.kind==='project'){$('[data-default-provider]').onchange=e=>{provider=e.target.value;dirty=false;render();};$('[data-default-playbook]').onchange=()=>mark(true);$('[data-save-default]').onclick=saveDefault;}}
 async function save(event){event.preventDefault();if(pending)return;const form=new FormData(event.currentTarget),scopeValue=String(form.get('scope')),entryScope=scopeValue==='global'?{kind:'global'}:{kind:'project',projectID:scopeValue.split(':')[1]},providers=form.getAll('provider'),skillIDs=[...host.querySelectorAll('[data-editor-skill]:checked')].map(input=>input.dataset.editorSkill),connectionIDs=[...host.querySelectorAll('[data-editor-connection]:checked')].map(input=>input.dataset.editorConnection);pending=true;try{await api(editor.item.id?'playbooks/'+editor.item.id:'playbooks',editor.item.id?'PUT':'POST',{revision:state.revision,...(editor.item.id?{version:editor.item.version}:{creationRequestKey:editor.requestKey}),name:form.get('name'),description:form.get('description'),scope:entryScope,providers,skillIDs,connectionIDs});notify?.('Playbook saved.');await load();}catch(error){fail(error);pending=false;mark(true);}finally{pending=false;}}
 async function archive(){pending=true;try{await api(`playbooks/${selected.id}/archive`,'POST',{revision:state.revision,version:selected.version,archived:!selected.archived});await load();}catch(error){fail(error);}finally{pending=false;}}
 async function duplicate(){pending=true;try{await api(`playbooks/${selected.id}/duplicate`,'POST',{revision:state.revision,name:`${selected.name} copy`,creationRequestKey:crypto.randomUUID()});notify?.('Playbook duplicated.');await load();}catch(error){fail(error);}finally{pending=false;}}
 async function saveDefault(){const item=currentDefault(),playbookID=$('[data-default-playbook]').value;pending=true;try{await api('playbooks/defaults','PUT',{revision:state.revision,defaultRevision:item.revision,projectID:scope.projectID,provider,playbookID});dirty=false;notify?.('Project default saved.');await load();}catch(error){fail(error);pending=false;mark(true);}finally{pending=false;}}
 load();return {dispose(){disposed=true;},isDirty:()=>dirty,isPending:()=>pending};
}
