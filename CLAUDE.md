# SKD Workbench

Read and follow the shared repository guidance in @AGENTS.md.

It contains setup commands, the source map, persistence and execution invariants,
validation requirements, and Git/Tracker Trapper conventions. Keep shared guidance
in `AGENTS.md` so Codex and Claude use the same project instructions.
