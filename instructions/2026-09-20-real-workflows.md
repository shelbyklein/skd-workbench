# Real Codex workflows

Continue authorized solo implementation after 9114b1e: execute saved flows through Codex, retain outputs and token usage, pause for human review, repeat bounded sections on requested changes. No second provider, merge/push, dollar budgets or automatic quality claims. Existing simulations and one-task runs remain usable. No remote repository configured; track locally in TT.

- [x] TT-WORKFLOW-01: Persist immutable flow/config/project snapshots and sequential runner. Share one isolated coding worktree across agent attempts; read-only mode uses connected folder. Explicit per-step Codex mapping; no substitution of Fable/Opus labels. Human reviews pause and can return to a configured prior agent; Check steps are manual evidence gates, never fabricated automated checks. Limits: 30 flow steps, existing per-review retry bounds, maximum 1–60 total agent attempts (default20), each child retains runtime/output limits. Handoff cap128k chars: stop visibly rather than silently truncate. Failure/stop interrupts the sequence; restarting the server never automatically relaunches paid work. Acceptance: fixtures test state/revisions, shared worktree, handoffs, retained usage, stale actions, failures, cancellation and recovery.
- [x] TT-WORKFLOW-02: Add Run with Codex to saved-flow editor, explicit model/effort mapping dialog, real flow history and live connected steps with outputs, review decisions and totals. Keep simulation labeled. Acceptance: Chrome entry, gates/retries, draft preservation, history/reload, keyboard/mobile and escaped output.
- [x] TT-WORKFLOW-03: Real Codex multi-step smoke in fixture project, regression suites, inspected screenshots, docs, source commit and activation at4390. Preserve user data; update PWA cache.

Architecture: lib/workflows.js owns state machine in separate workflows.json; lib/codex.js remains subprocess adapter with internal workflow ownership and workspace reuse. Server validates registered project/flow version; workflow snapshots are immutable. Child records store workflow/attempt IDs to reconcile crash gaps. A reserved workflow blocks other execution even while awaiting review; stop/release before a separate run. A source folder or saved flow edit never changes an in-flight snapshot. Browser polls state; transitions require exact revisions. All paid attempts counted, including retried/failed attempts; totals disclose unknown usage.

Delivery: see VALIDATION.md. Real two-agent Codex workflow and both review gates verified; same-worktree handoff/file creation and original-source preservation checked. Live v0.5.0 at4390; existing store unchanged. No second provider or real-run comparison UI claimed.
