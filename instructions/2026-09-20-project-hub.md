# Home → Project Overview → Workflows

Confirmed user correction: Home is a list of project cards. Each project has its own overview of available views; Workflows is one of those views and keeps its own overview. Implement now, solo. Preserve saved data, runs and existing editor URLs; start no real executions during validation.

SKD-HUB-01: add the missing project landing page, make Home/project/workflow routes distinct, provide links to available Workflows and standalone Codex views, show Issues/Scratchpad/Knowledge as planned unavailable views, retain contextual sidebars and dirty-edit protection, and validate route reload/navigation/mobile in browser fixtures. Inspect actual localhost read-only and confirm saved state/history unchanged. Static UI activation with PWA cache update; local commit.

Do not implement the planned views or imply their capabilities are ready. This is navigation only, not completion of the development-lanes roadmap.

TT: local:E1AD8C8F-AE2D-46D2-A8A1-D7B5F4413A84. No open decisions or further approval needed for this scope.

Completed: all nine browser suites passed. Live read-only traversal and inspected screenshot confirm the three levels; `output/project-hub-live-receipt.json` records zero execution requests and unchanged state/history. PWA cache is `skd-shell-0.5.0-6`. Future views remain planned.
